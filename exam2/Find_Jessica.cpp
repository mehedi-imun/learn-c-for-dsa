#include <bits/stdc++.h>
using namespace std;

int main()
{
    string s;
    getline(cin, s);
    stringstream ss(s);
    int flag = 0;
  while (ss>>s)
  {
   if(s=="Jessica")
   {
       flag
   }
   else
   {
       cout<<"No";
   }
  }
  
    return 0;
}